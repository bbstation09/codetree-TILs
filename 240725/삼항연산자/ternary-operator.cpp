#include <iostream>
#include <string>
using namespace std;

int main() {
    // 여기에 코드를 작성해주세요.
    int score;
    string result;
    cin>>score;

    result = (grade=0) ? "pass" : "failure";

    cout<<result;
    return 0;
}